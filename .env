

# Docker Compose build stability for Docker Desktop / containerd
# Parallel builds can trigger overlayfs snapshot corruption on some hosts.
COMPOSE_PARALLEL_LIMIT=1

# Database Configuration
DB_PASSWORD="Dhakool1$"

# JWT Configuration
JWT_SECRET="px8Ua5EDv5ra2rDlqY4V7Ay3BTEJ0EKhgBUb6CAx1l2"

# SMTP Configuration (for email verification)

SMTP_HOST="smtp.gmail.com"

# SMTP port (typically 587 for TLS, 465 for SSL)
SMTP_PORT=587

# SMTP username for authentication
SMTP_USERNAME="uthmanbusayomi@gmail.com"

# SMTP password for authentication
# Note: For Gmail, use App Password, not account password
SMTP_PASSWORD="bfcm jeeh opdr iof"

# Optional: SMTP from address (defaults to SMTP_USERNAME if not set)
SMTP_FROM_ADDRESS="noreply@tradealert.com"

# ============================================================================
# Advanced Configuration (Optional - Leave as defaults if unsure)
# ============================================================================

# Spring Profiles (prod, dev, test)
SPRING_PROFILES_ACTIVE=prod

# Kubernetes/Docker networking (internal service discovery)
# These are automatically resolved in docker-compose networks
# SPRING_DATASOURCE_URL=jdbc:postgresql://postgres-primary:5432/tradealert
# SPRING_KAFKA_BOOTSTRAP_SERVERS=kafka:9092
# SPRING_REDIS_HOST=redis

# Connection Pool Configuration
# ============================================================================
# HikariCP connection pool settings (already configured in docker-compose.yml)
# DB_HIKARI_POOL_SIZE=20
# DB_HIKARI_MIN_IDLE=5
# DB_HIKARI_CONNECTION_TIMEOUT=5000

# Monitoring & Logging
# ============================================================================
# Prometheus scrape interval (docker-compose uses 10s)
# PROMETHEUS_SCRAPE_INTERVAL=10s

# Log level (DEBUG, INFO, WARN, ERROR)
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_COM_TRADEALERT=DEBUG


# 3. Start containers:
#    docker-compose up -d
#
# 4. Verify health:
#    curl http://localhost:8080/actuator/health     # user-service
#    curl http://localhost:8081/actuator/health     # api-gateway
#    curl http://localhost:9090                      # prometheus
#
# ============================================================================
