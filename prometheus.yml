global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: tradealert
    environment: local

scrape_configs:
  - job_name: tradealert-app
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: [tradealert-app:8080]
    scrape_interval: 10s
