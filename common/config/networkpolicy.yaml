apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: tradealert-network-policy
  namespace: tradealert
spec:
  podSelector: {}
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector: {}
  egress:
    - to:
        - podSelector: {}
